import React from "react";
import Grafico from "../socketgrafico";

const Graficopage = () => (
    <div className="title">
        <p>Página do gráfico.</p>
         <Grafico/>     
    </div>
);

export default Graficopage;